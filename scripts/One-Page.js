$(function(){
/*
    var containers = [
        [$( ".main" ),      $( "#main_pos" ),    ],
        [$( ".lsf" ),       $( "#lsf_pos" ),     ],
        [$( ".sim" ),       $( "#sim_pos" ),     ],
        [$( ".fhem" ),      $( "#fhem_pos" ),    ],
        [$( ".tablet" ),    $( "#tablet_pos" ),  ],
        [$( ".team" ),      $( "#team_pos" ),    ],
        [$( ".contact" ),   $( "#contact_pos" ), ],
        [$( ".impressum" ), $( "#impressum_pos" )],
        [$( ".dsgvo" ),     $( "#dsgvo_pos" ),   ]
    ];

    //Laden und Entladen
    var preload = 20;
    window.onscroll = function() {
        containers.forEach(function(item, index, array) {
            var elementOffsetTop = item[1].offset().top;
            var windowOffsetBottom = $(window).scrollTop() + $(window).height();
            if (elementOffsetTop < windowOffsetBottom + preload) {
                item[0].show();
            }
            else {
                item[0].hide();
            }
          });
    }  
*/
});